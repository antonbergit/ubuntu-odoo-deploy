odoo-install sequence

1. Install scripts
2. Create non-root user
3. odoo-install
4. chnod
5. odoo-helper-server start

wget -O /tmp/odoo-helper-install.bash https://gitlab.com/katyukha/odoo-helper-scripts/raw/master/install-system.bash;

# Install odoo-helper-scripts
sudo bash /tmp/odoo-helper-install.bash;
odoo-helper install postgres
#  Intall system pre-requirements for odoo-helper-scripts
# NOTE: Works only on debian-based systems
odoo-helper install pre-requirements



i.3. [sodo] odoo-install --install-dir <directory to install like /opt/odoo-16.0> --odoo-version <versoin number like 16.0> --single-branch on --build-python 3.8.10 --db-host localhost --db-user odoo --db-pass odoo --http-port 8669 --conf-opt-xmlrpc_port 8669 --conf-opt-admin_passwd PASSWD --sys-deps --ikwid
sudo odoo-install --install-dir /opt/profigas-16-ce --odoo-version 16.0 --single-branch on --build-python 3.8.10 --db-host localhost --db-user odoo --db-pass odoo --conf-opt-admin_passwd SpocOdooAdmin --sys-deps --ikwid
